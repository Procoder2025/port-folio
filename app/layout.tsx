import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Karthikeyan | AI Developer & UI/UX Designer",
  description:
    "Portfolio of Karthikeyan — AI Developer, UI/UX Designer & Full-Stack Web Developer building futuristic digital experiences.",
  keywords: ["AI Developer", "UI/UX Designer", "Portfolio", "Next.js", "Three.js", "Karthikeyan"],
  openGraph: {
    title: "Karthikeyan | AI Developer & UI/UX Designer",
    description: "Building the future with AI, design, and code.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <head>
        <link
          href="https://fonts.googleapis.com/css2?family=Syne:wght@400;500;600;700;800&family=DM+Sans:ital,opsz,wght@0,9..40,300;0,9..40,400;0,9..40,500;0,9..40,600;0,9..40,700;1,9..40,400&display=swap"
          rel="stylesheet"
        />
      </head>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
